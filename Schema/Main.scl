//! Reusable schema constructs. Consumers import Self/Decode (or the modules a
//! deployment of this repository exports) rather than this entrypoint, which
//! deploys nothing by itself.

import Self/Decode
